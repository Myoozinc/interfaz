export const MARIO_KART_GAME_HTML = "<!DOCTYPE html>\n<html lang=\"es\">\n<head>\n  <meta charset=\"UTF-8\">\n  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0, user-scalable=no\">\n  <title>Mario Kart 3D Arcade GP</title>\n  <script src=\"https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js\"></script>\n  <script src=\"https://cdn.tailwindcss.com\"></script>\n  <script src=\"https://unpkg.com/lucide@latest\"></script>\n  <style>\n    body { margin: 0; overflow: hidden; background: #38bdf8; touch-action: manipulation; }\n    canvas { display: block; width: 100vw; height: 100vh; }\n    .arcade-card { background: rgba(255, 255, 255, 0.88); backdrop-filter: blur(8px); border: 2px solid #ffffff; box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2); }\n    .btn-touch:active { transform: scale(0.92); filter: brightness(0.9); }\n    @keyframes bobble { 0%, 100% { transform: translateY(0px); } 50% { transform: translateY(-6px); } }\n    .animate-bobble { animation: bobble 1.8s ease-in-out infinite; }\n  </style>\n</head>\n<body class=\"select-none font-sans text-slate-900\">\n\n  <!-- 3D Canvas -->\n  <div id=\"canvas-container\" class=\"absolute inset-0\"></div>\n\n  <!-- Top Arcade HUD -->\n  <header class=\"absolute top-3 left-3 right-3 flex justify-between items-start pointer-events-none z-10\">\n    <!-- Left: Position & Lap -->\n    <div class=\"arcade-card px-3.5 py-2 rounded-2xl flex items-center gap-3 pointer-events-auto\">\n      <div class=\"w-10 h-10 rounded-xl bg-gradient-to-tr from-amber-400 to-red-500 flex items-center justify-center text-white font-black text-xl shadow-md\">\n        1º\n      </div>\n      <div>\n        <h1 class=\"text-xs font-black tracking-wider text-red-600 uppercase flex items-center gap-1\">\n          <span>🏎️ CIRCUITO PRADERA</span>\n        </h1>\n        <div class=\"flex items-center gap-2 text-[11px] font-bold text-slate-700\">\n          <span>VUELTA: <span id=\"hud-lap\" class=\"text-red-600 font-black\">1/3</span></span>\n          <span>•</span>\n          <span class=\"flex items-center gap-0.5 text-amber-600\">🪙 <span id=\"hud-coins\" class=\"font-black\">0</span></span>\n        </div>\n      </div>\n    </div>\n\n    <!-- Right: Speedometer & Turbo -->\n    <div class=\"arcade-card px-4 py-2 rounded-2xl pointer-events-auto text-right flex items-center gap-3\">\n      <div>\n        <div class=\"flex items-baseline justify-end gap-1\">\n          <span id=\"hud-speed\" class=\"text-3xl font-black text-red-600 font-mono tracking-tight leading-none\">0</span>\n          <span class=\"text-[10px] font-black text-slate-400\">KM/H</span>\n        </div>\n        <div class=\"flex items-center gap-1 mt-1 justify-end\">\n          <span class=\"text-[10px] font-black text-emerald-600\">🍄 TURBO:</span>\n          <div class=\"w-16 bg-slate-200 h-2 rounded-full overflow-hidden border border-slate-300\">\n            <div id=\"hud-turbo-bar\" class=\"bg-gradient-to-r from-emerald-400 to-emerald-600 h-full w-full transition-all duration-75\"></div>\n          </div>\n        </div>\n      </div>\n    </div>\n  </header>\n\n  <!-- Notification Toast -->\n  <div id=\"hud-toast\" class=\"absolute top-20 left-1/2 -translate-x-1/2 arcade-card px-4 py-1.5 rounded-full text-xs font-black text-red-600 pointer-events-none opacity-0 transition-opacity duration-300 z-20 shadow-lg flex items-center gap-1.5\">\n    <span>🍄 ¡SUPER TURBO ACTIVADO!</span>\n  </div>\n\n  <!-- Start & Game Over Screen -->\n  <div id=\"start-overlay\" class=\"absolute inset-0 bg-sky-900/60 backdrop-blur-sm flex items-center justify-center z-30 transition-opacity duration-300\">\n    <div class=\"arcade-card p-6 sm:p-8 rounded-3xl max-w-sm w-full mx-4 text-center shadow-2xl border-4 border-white animate-bobble\">\n      <div class=\"w-16 h-16 rounded-2xl bg-gradient-to-tr from-red-500 to-amber-400 flex items-center justify-center text-white text-3xl mx-auto mb-3 shadow-lg shadow-red-500/40\">\n        🏎️\n      </div>\n      <h2 class=\"text-2xl font-black text-slate-900 mb-1 tracking-tight\">KART GP ARCADE 3D</h2>\n      <p class=\"text-xs text-slate-600 mb-5 font-semibold\">¡Compite en el circuito pradera, recoge monedas y usa los turbos para ganar!</p>\n\n      <div class=\"bg-amber-50 border border-amber-200 rounded-xl p-2.5 text-[11px] text-amber-900 font-bold mb-5 text-left space-y-1\">\n        <p>🎮 <b>Teclado:</b> [W / ↑] Acelerar • [S / ↓] Frenar • [A / D] Girar</p>\n        <p>🍄 <b>Acciones:</b> [ESPACIO] Derrape • [N] Turbo Nitro</p>\n      </div>\n\n      <button id=\"btn-start\" class=\"w-full py-3.5 px-6 rounded-2xl bg-gradient-to-r from-red-500 to-orange-500 hover:from-red-600 hover:to-orange-600 text-white font-black text-sm tracking-wide uppercase shadow-lg shadow-red-500/40 transition-all transform active:scale-95 cursor-pointer\">\n        🏁 ¡ARRANCAR CARRERA!\n      </button>\n    </div>\n  </div>\n\n  <!-- Mobile On-Screen Controls -->\n  <div class=\"absolute bottom-4 left-4 right-4 flex justify-between items-end pointer-events-none z-20 sm:hidden\">\n    <!-- Steering Left / Right -->\n    <div class=\"flex gap-2 pointer-events-auto\">\n      <button id=\"btn-left\" class=\"btn-touch w-14 h-14 rounded-2xl bg-white/90 border-2 border-slate-200 shadow-xl flex items-center justify-center text-slate-800 font-black text-2xl active:bg-amber-100\">\n        ◀\n      </button>\n      <button id=\"btn-right\" class=\"btn-touch w-14 h-14 rounded-2xl bg-white/90 border-2 border-slate-200 shadow-xl flex items-center justify-center text-slate-800 font-black text-2xl active:bg-amber-100\">\n        ▶\n      </button>\n    </div>\n\n    <!-- Action Buttons (Gas, Brake, Nitro) -->\n    <div class=\"flex gap-2 pointer-events-auto\">\n      <button id=\"btn-drift\" class=\"btn-touch w-12 h-12 rounded-2xl bg-amber-400 border-2 border-amber-300 shadow-xl flex items-center justify-center text-white font-black text-xs shadow-amber-400/40\">\n        DRIFT\n      </button>\n      <button id=\"btn-brake\" class=\"btn-touch w-13 h-13 rounded-2xl bg-red-500 border-2 border-red-400 shadow-xl flex items-center justify-center text-white font-black text-xs shadow-red-500/40\">\n        FRENO\n      </button>\n      <button id=\"btn-gas\" class=\"btn-touch w-16 h-16 rounded-2xl bg-emerald-500 border-2 border-emerald-400 shadow-xl flex items-center justify-center text-white font-black text-sm shadow-emerald-500/40\">\n        GAS!\n      </button>\n    </div>\n  </div>\n\n  <script>\n    let scene, camera, renderer, kartGroup;\n    let wheels = [];\n    let smokeParticles, coinObjects = [], turboPads = [], clouds = [];\n    let isRunning = false;\n    let speed = 0, maxSpeed = 0.55, accel = 0.015, friction = 0.985, steeringAngle = 0;\n    let turboFuel = 100, isTurboActive = false, turboTimer = 0;\n    let coinsCollected = 0, currentLap = 1, totalDistance = 0, lapDistance = 1400;\n    let audioCtx = null, engineOsc = null, engineGain = null;\n\n    const keys = { forward: false, backward: false, left: false, right: false, drift: false };\n\n    function initAudio() {\n      try {\n        const AudioClass = window.AudioContext || window.webkitAudioContext;\n        audioCtx = new AudioClass();\n\n        engineOsc = audioCtx.createOscillator();\n        engineOsc.type = 'sawtooth';\n        engineOsc.frequency.setValueAtTime(55, audioCtx.currentTime);\n\n        const filter = audioCtx.createBiquadFilter();\n        filter.type = 'lowpass';\n        filter.frequency.setValueAtTime(450, audioCtx.currentTime);\n\n        engineGain = audioCtx.createGain();\n        engineGain.gain.setValueAtTime(0.08, audioCtx.currentTime);\n\n        engineOsc.connect(filter);\n        filter.connect(engineGain);\n        engineGain.connect(audioCtx.destination);\n        engineOsc.start();\n      } catch (e) {\n        console.warn('AudioContext not supported');\n      }\n    }\n\n    function playSfx(type) {\n      if (!audioCtx) return;\n      const now = audioCtx.currentTime;\n      const osc = audioCtx.createOscillator();\n      const gain = audioCtx.createGain();\n      osc.connect(gain);\n      gain.connect(audioCtx.destination);\n\n      if (type === 'coin') {\n        osc.type = 'sine';\n        osc.frequency.setValueAtTime(987, now);\n        osc.frequency.setValueAtTime(1318, now + 0.08);\n        gain.gain.setValueAtTime(0.2, now);\n        gain.gain.exponentialRampToValueAtTime(0.001, now + 0.3);\n        osc.start(now);\n        osc.stop(now + 0.3);\n      } else if (type === 'turbo') {\n        osc.type = 'triangle';\n        osc.frequency.setValueAtTime(250, now);\n        osc.frequency.exponentialRampToValueAtTime(880, now + 0.4);\n        gain.gain.setValueAtTime(0.3, now);\n        gain.gain.exponentialRampToValueAtTime(0.001, now + 0.5);\n        osc.start(now);\n        osc.stop(now + 0.5);\n      }\n    }\n\n    function init3D() {\n      const container = document.getElementById('canvas-container');\n      scene = new THREE.Scene();\n      scene.background = new THREE.Color(0x38bdf8);\n      scene.fog = new THREE.Fog(0x38bdf8, 60, 220);\n\n      camera = new THREE.PerspectiveCamera(60, window.innerWidth / window.innerHeight, 0.1, 1000);\n      camera.position.set(0, 3.2, 6.5);\n\n      renderer = new THREE.WebGLRenderer({ antialias: true });\n      renderer.setSize(window.innerWidth, window.innerHeight);\n      renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));\n      renderer.shadowMap.enabled = true;\n      container.appendChild(renderer.domElement);\n\n      // Lights\n      const ambientLight = new THREE.AmbientLight(0xffffff, 0.7);\n      scene.add(ambientLight);\n\n      const sunLight = new THREE.DirectionalLight(0xfff8e7, 0.9);\n      sunLight.position.set(40, 70, 30);\n      sunLight.castShadow = true;\n      scene.add(sunLight);\n\n      buildTrack();\n      buildKart();\n      buildClouds();\n      buildScenery();\n      buildPickups();\n      setupControls();\n\n      window.addEventListener('resize', onWindowResize);\n      requestAnimationFrame(animate);\n    }\n\n    function buildTrack() {\n      // Endless ground plane (green hills)\n      const groundGeo = new THREE.PlaneGeometry(800, 800, 32, 32);\n      groundGeo.rotateX(-Math.PI / 2);\n      const groundMat = new THREE.MeshStandardMaterial({ color: 0x48bb78, roughness: 0.9 });\n      const ground = new THREE.Mesh(groundGeo, groundMat);\n      ground.position.y = -0.05;\n      ground.receiveShadow = true;\n      scene.add(ground);\n\n      // Asphalt Road\n      const roadGeo = new THREE.PlaneGeometry(16, 1200);\n      roadGeo.rotateX(-Math.PI / 2);\n      const roadMat = new THREE.MeshStandardMaterial({ color: 0x334155, roughness: 0.8 });\n      const road = new THREE.Mesh(roadGeo, roadMat);\n      road.receiveShadow = true;\n      scene.add(road);\n\n      // Red and white curbs\n      for (let side of [-8, 8]) {\n        for (let z = -550; z < 550; z += 8) {\n          const isRed = Math.floor(z / 8) % 2 === 0;\n          const curbGeo = new THREE.BoxGeometry(0.8, 0.25, 8);\n          const curbMat = new THREE.MeshStandardMaterial({ color: isRed ? 0xef4444 : 0xffffff });\n          const curb = new THREE.Mesh(curbGeo, curbMat);\n          curb.position.set(side, 0.1, z);\n          scene.add(curb);\n        }\n      }\n\n      // White dash line in center\n      for (let z = -550; z < 550; z += 14) {\n        const lineGeo = new THREE.PlaneGeometry(0.35, 7);\n        lineGeo.rotateX(-Math.PI / 2);\n        const lineMat = new THREE.MeshBasicMaterial({ color: 0xffffff });\n        const line = new THREE.Mesh(lineGeo, lineMat);\n        line.position.set(0, 0.01, z);\n        scene.add(line);\n      }\n    }\n\n    function buildKart() {\n      kartGroup = new THREE.Group();\n\n      // Red Aerodynamic Body\n      const bodyGeo = new THREE.BoxGeometry(1.4, 0.45, 2.2);\n      const bodyMat = new THREE.MeshStandardMaterial({ color: 0xe11d48, roughness: 0.3, metalness: 0.2 });\n      const body = new THREE.Mesh(bodyGeo, bodyMat);\n      body.position.y = 0.42;\n      body.castShadow = true;\n      kartGroup.add(body);\n\n      // White Center Racing Stripe\n      const stripeGeo = new THREE.BoxGeometry(0.35, 0.47, 2.22);\n      const stripeMat = new THREE.MeshStandardMaterial({ color: 0xffffff });\n      const stripe = new THREE.Mesh(stripeGeo, stripeMat);\n      stripe.position.y = 0.42;\n      kartGroup.add(stripe);\n\n      // Cockpit & Seat\n      const seatGeo = new THREE.BoxGeometry(0.8, 0.6, 0.7);\n      const seatMat = new THREE.MeshStandardMaterial({ color: 0x1e293b });\n      const seat = new THREE.Mesh(seatGeo, seatMat);\n      seat.position.set(0, 0.65, 0.2);\n      kartGroup.add(seat);\n\n      // Steering Wheel\n      const wheelRingGeo = new THREE.TorusGeometry(0.2, 0.04, 8, 16);\n      const wheelRingMat = new THREE.MeshStandardMaterial({ color: 0xfacc15, metalness: 0.6 });\n      const steering = new THREE.Mesh(wheelRingGeo, wheelRingMat);\n      steering.position.set(0, 0.75, -0.4);\n      steering.rotation.x = Math.PI / 4;\n      kartGroup.add(steering);\n\n      // Rear Spoiler\n      const spoilerWingGeo = new THREE.BoxGeometry(1.6, 0.08, 0.4);\n      const spoilerMat = new THREE.MeshStandardMaterial({ color: 0xe11d48 });\n      const spoilerWing = new THREE.Mesh(spoilerWingGeo, spoilerMat);\n      spoilerWing.position.set(0, 1.0, 1.1);\n      kartGroup.add(spoilerWing);\n\n      const standL = new THREE.Mesh(new THREE.CylinderGeometry(0.04, 0.04, 0.5), new THREE.MeshStandardMaterial({ color: 0x94a3b8 }));\n      standL.position.set(-0.5, 0.75, 1.1);\n      kartGroup.add(standL);\n      const standR = standL.clone();\n      standR.position.x = 0.5;\n      kartGroup.add(standR);\n\n      // Exhaust Pipes\n      const exhaustGeo = new THREE.CylinderGeometry(0.09, 0.09, 0.4, 12);\n      exhaustGeo.rotateX(Math.PI / 2);\n      const exhaustMat = new THREE.MeshStandardMaterial({ color: 0x64748b, metalness: 0.8 });\n      const exL = new THREE.Mesh(exhaustGeo, exhaustMat);\n      exL.position.set(-0.35, 0.35, 1.25);\n      kartGroup.add(exL);\n      const exR = exL.clone();\n      exR.position.x = 0.35;\n      kartGroup.add(exR);\n\n      // 4 Chunky Cartoon Wheels\n      const tireGeo = new THREE.CylinderGeometry(0.35, 0.35, 0.3, 18);\n      tireGeo.rotateZ(Math.PI / 2);\n      const tireMat = new THREE.MeshStandardMaterial({ color: 0x0f172a, roughness: 0.8 });\n      const rimMat = new THREE.MeshStandardMaterial({ color: 0xfacc15, metalness: 0.4 });\n\n      const wheelCoords = [\n        { x: -0.9, y: 0.35, z: -0.7 },\n        { x: 0.9, y: 0.35, z: -0.7 },\n        { x: -0.9, y: 0.35, z: 0.7 },\n        { x: 0.9, y: 0.35, z: 0.7 }\n      ];\n\n      wheelCoords.forEach(pos => {\n        const wMesh = new THREE.Mesh(tireGeo, tireMat);\n        wMesh.position.set(pos.x, pos.y, pos.z);\n        wMesh.castShadow = true;\n\n        const rim = new THREE.Mesh(new THREE.CylinderGeometry(0.18, 0.18, 0.32, 12), rimMat);\n        rim.rotation.z = Math.PI / 2;\n        wMesh.add(rim);\n\n        kartGroup.add(wMesh);\n        wheels.push(wMesh);\n      });\n\n      kartGroup.position.set(0, 0, 0);\n      scene.add(kartGroup);\n    }\n\n    function buildClouds() {\n      for (let i = 0; i < 24; i++) {\n        const cloudGroup = new THREE.Group();\n        const puffCount = 3 + Math.floor(Math.random() * 3);\n        const cloudMat = new THREE.MeshStandardMaterial({ color: 0xffffff, roughness: 0.5 });\n        for (let p = 0; p < puffCount; p++) {\n          const r = 3 + Math.random() * 2.5;\n          const puff = new THREE.Mesh(new THREE.DodecahedronGeometry(r, 1), cloudMat);\n          puff.position.set((p - puffCount / 2) * 3, Math.random() * 1.5, Math.random() * 2);\n          cloudGroup.add(puff);\n        }\n        cloudGroup.position.set(\n          (Math.random() - 0.5) * 280,\n          35 + Math.random() * 25,\n          (Math.random() - 0.5) * 600\n        );\n        scene.add(cloudGroup);\n        clouds.push(cloudGroup);\n      }\n    }\n\n    function buildScenery() {\n      // Cartoon Pine Trees along the grass\n      const trunkGeo = new THREE.CylinderGeometry(0.3, 0.4, 2, 8);\n      const trunkMat = new THREE.MeshStandardMaterial({ color: 0x78350f });\n      const foliageGeo = new THREE.ConeGeometry(2.2, 4.5, 8);\n      const foliageMat = new THREE.MeshStandardMaterial({ color: 0x16a34a, roughness: 0.8 });\n\n      for (let z = -500; z < 500; z += 25) {\n        for (let side of [-16 - Math.random() * 18, 16 + Math.random() * 18]) {\n          const tree = new THREE.Group();\n          const trunk = new THREE.Mesh(trunkGeo, trunkMat);\n          trunk.position.y = 1;\n          tree.add(trunk);\n\n          const foliage = new THREE.Mesh(foliageGeo, foliageMat);\n          foliage.position.y = 3.8;\n          tree.add(foliage);\n\n          tree.position.set(side, 0, z + (Math.random() - 0.5) * 10);\n          scene.add(tree);\n        }\n      }\n    }\n\n    function buildPickups() {\n      // Floating Gold Coins\n      const coinGeo = new THREE.CylinderGeometry(0.5, 0.5, 0.12, 16);\n      coinGeo.rotateX(Math.PI / 2);\n      const coinMat = new THREE.MeshStandardMaterial({ color: 0xfbbf24, metalness: 0.8, roughness: 0.2 });\n\n      for (let z = -450; z < 450; z += 35) {\n        const lane = (Math.random() - 0.5) * 10;\n        const coin = new THREE.Mesh(coinGeo, coinMat);\n        coin.position.set(lane, 0.9, z);\n        scene.add(coin);\n        coinObjects.push(coin);\n      }\n\n      // Turbo Boost Pads\n      const padGeo = new THREE.PlaneGeometry(3.5, 6);\n      padGeo.rotateX(-Math.PI / 2);\n      const padMat = new THREE.MeshBasicMaterial({ color: 0xf97316 });\n\n      for (let z = -400; z < 400; z += 120) {\n        const lane = (Math.random() - 0.5) * 8;\n        const pad = new THREE.Mesh(padGeo, padMat);\n        pad.position.set(lane, 0.02, z);\n        scene.add(pad);\n        turboPads.push(pad);\n      }\n    }\n\n    function setupControls() {\n      window.addEventListener('keydown', (e) => {\n        if (e.key === 'w' || e.key === 'W' || e.key === 'ArrowUp') keys.forward = true;\n        if (e.key === 's' || e.key === 'S' || e.key === 'ArrowDown') keys.backward = true;\n        if (e.key === 'a' || e.key === 'A' || e.key === 'ArrowLeft') keys.left = true;\n        if (e.key === 'd' || e.key === 'D' || e.key === 'ArrowRight') keys.right = true;\n        if (e.code === 'Space') keys.drift = true;\n        if (e.key === 'n' || e.key === 'N') triggerTurbo();\n      });\n\n      window.addEventListener('keyup', (e) => {\n        if (e.key === 'w' || e.key === 'W' || e.key === 'ArrowUp') keys.forward = false;\n        if (e.key === 's' || e.key === 'S' || e.key === 'ArrowDown') keys.backward = false;\n        if (e.key === 'a' || e.key === 'A' || e.key === 'ArrowLeft') keys.left = false;\n        if (e.key === 'd' || e.key === 'D' || e.key === 'ArrowRight') keys.right = false;\n        if (e.code === 'Space') keys.drift = false;\n      });\n\n      const bindTouch = (id, keyName) => {\n        const el = document.getElementById(id);\n        if (!el) return;\n        const press = (e) => { e.preventDefault(); keys[keyName] = true; };\n        const release = (e) => { e.preventDefault(); keys[keyName] = false; };\n        el.addEventListener('touchstart', press, { passive: false });\n        el.addEventListener('touchend', release);\n        el.addEventListener('mousedown', press);\n        el.addEventListener('mouseup', release);\n        el.addEventListener('mouseleave', release);\n      };\n\n      bindTouch('btn-gas', 'forward');\n      bindTouch('btn-brake', 'backward');\n      bindTouch('btn-left', 'left');\n      bindTouch('btn-right', 'right');\n      bindTouch('btn-drift', 'drift');\n\n      document.getElementById('btn-start').addEventListener('click', () => {\n        initAudio();\n        document.getElementById('start-overlay').style.opacity = '0';\n        setTimeout(() => {\n          document.getElementById('start-overlay').style.display = 'none';\n        }, 300);\n        isRunning = true;\n      });\n    }\n\n    function triggerTurbo() {\n      if (turboFuel < 30 || isTurboActive) return;\n      isTurboActive = true;\n      turboTimer = 180; // 3 seconds at 60fps\n      turboFuel = Math.max(0, turboFuel - 45);\n      playSfx('turbo');\n\n      const toast = document.getElementById('hud-toast');\n      toast.style.opacity = '1';\n      setTimeout(() => { toast.style.opacity = '0'; }, 1500);\n    }\n\n    function animate() {\n      requestAnimationFrame(animate);\n\n      if (isRunning) {\n        // Physics & Acceleration\n        const currentMaxSpeed = isTurboActive ? maxSpeed * 1.6 : maxSpeed;\n        if (keys.forward) {\n          speed = Math.min(speed + accel, currentMaxSpeed);\n        } else if (keys.backward) {\n          speed = Math.max(speed - accel * 0.7, -maxSpeed * 0.4);\n        } else {\n          speed *= friction;\n        }\n\n        // Steering\n        const turnSpeed = keys.drift ? 0.06 : 0.04;\n        if (keys.left) {\n          kartGroup.position.x -= speed * 1.4;\n          steeringAngle = Math.min(steeringAngle + 0.08, 0.4);\n        } else if (keys.right) {\n          kartGroup.position.x += speed * 1.4;\n          steeringAngle = Math.max(steeringAngle - 0.08, -0.4);\n        } else {\n          steeringAngle *= 0.85;\n        }\n\n        // Keep kart within track boundaries with grass friction\n        if (Math.abs(kartGroup.position.x) > 7.5) {\n          speed *= 0.94; // Grass slows you down!\n        }\n        kartGroup.position.x = Math.max(-14, Math.min(14, kartGroup.position.x));\n        kartGroup.rotation.y = -steeringAngle * 0.7;\n\n        // Rotate 4 wheels\n        wheels.forEach(w => {\n          w.rotation.x += speed * 1.2;\n        });\n\n        // Move the World relative to Kart (Endless race illusion)\n        totalDistance += speed * 45;\n        if (totalDistance >= lapDistance * currentLap && currentLap < 3) {\n          currentLap++;\n          document.getElementById('hud-lap').innerText = currentLap + '/3';\n        }\n\n        // Move Coins toward kart\n        coinObjects.forEach(c => {\n          c.rotation.z += 0.05;\n          c.position.z += speed * 35;\n          if (c.position.z > 15) c.position.z -= 900;\n\n          // Pickup Collision\n          if (Math.abs(c.position.z - kartGroup.position.z) < 1.2 && Math.abs(c.position.x - kartGroup.position.x) < 1.2) {\n            coinsCollected++;\n            document.getElementById('hud-coins').innerText = coinsCollected;\n            playSfx('coin');\n            c.position.z -= 900; // Respawn further down\n          }\n        });\n\n        // Turbo Pads\n        turboPads.forEach(p => {\n          p.position.z += speed * 35;\n          if (p.position.z > 20) p.position.z -= 720;\n          if (Math.abs(p.position.z - kartGroup.position.z) < 2.0 && Math.abs(p.position.x - kartGroup.position.x) < 2.0) {\n            triggerTurbo();\n          }\n        });\n\n        // Drift / Turbo Timer countdown\n        if (isTurboActive) {\n          turboTimer--;\n          if (turboTimer <= 0) isTurboActive = false;\n        } else if (turboFuel < 100) {\n          turboFuel = Math.min(100, turboFuel + 0.15);\n        }\n        document.getElementById('hud-turbo-bar').style.width = turboFuel + '%';\n\n        // Update Engine Sound\n        if (engineOsc && audioCtx) {\n          const targetFreq = 45 + Math.abs(speed / maxSpeed) * 220;\n          engineOsc.frequency.setTargetAtTime(targetFreq, audioCtx.currentTime, 0.05);\n        }\n\n        // Camera Follow\n        camera.position.x += (kartGroup.position.x * 0.7 - camera.position.x) * 0.12;\n        camera.lookAt(kartGroup.position.x * 0.4, 0.9, -15);\n\n        // Update HUD\n        const kmh = Math.floor(Math.abs(speed / maxSpeed) * 140);\n        document.getElementById('hud-speed').innerText = kmh;\n      }\n\n      // Slowly drift clouds\n      clouds.forEach(cl => {\n        cl.position.x += 0.02;\n        if (cl.position.x > 150) cl.position.x = -150;\n      });\n\n      renderer.render(scene, camera);\n    }\n\n    function onWindowResize() {\n      camera.aspect = window.innerWidth / window.innerHeight;\n      camera.updateProjectionMatrix();\n      renderer.setSize(window.innerWidth, window.innerHeight);\n    }\n\n    document.addEventListener('DOMContentLoaded', () => {\n      lucide.createIcons();\n      init3D();\n    });\n  </script>\n</body>\n</html>";
